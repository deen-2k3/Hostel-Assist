import { Application } from "../models/complaintApplication.model";
import { Complaint } from "../models/complaint.model";

export const getApplicants=async(req,res)=>{
    try {
        const userId=req.id;
        const application=await Application.find
    } catch (error) {
        
    }
}